{
	"name": "rescripts",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"devDependencies": {
		"lerna": "^3.4.3"
	},
	"scripts": {
		"bootstrap": "lerna bootstrap",
		"example": "npm -C ./packages/example run start",
		"publish": "npm run lerna publish --yes --skip-npm",
		"postpublish": "git push && git push --tags"
	}
}
